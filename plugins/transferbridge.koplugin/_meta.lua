local _ = require("gettext")
return {
    name = "transferbridge",
    fullname = _("TransferBridge"),
    description = _([[WiFi drag-and-drop file transfer. Drop books and manga
from any browser on your local network directly to your device.]]),
}
